{
  "name": "giwa-receivable-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "solcjs --base-path . --include-path node_modules --abi --bin contracts/MockKRW.sol contracts/ReceivableFinance.sol --output-dir build",
    "test": "npm run compile && hardhat test --no-compile"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.4.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "3.1.0",
    "ethers": "6.17.0",
    "hardhat": "2.29.0",
    "solc": "0.8.24"
  }
}
